# 工具包自检报告

> 自检的价值不在于"跑通了"，而在于**能抓到自己的错**。下面的记录全是真跑出来的，包括三个被自检抓出来的真 bug。

## 怎么跑

```bash
python pcbai.py watch --once     # ① 先确认桥接活着、且已连上 EDA
python pcbai.py selftest         # ② 几何引擎 + 布线器 只读自检（不创建/不修改任何图元）
python pcbai.py holes            # ③ 全板钻孔可制造性（孔到孔 / 孔径 / 环宽）
```

`pcbai.py` 会自动把 `tools/geom_router.js` 前置注入到自检脚本里（自检脚本里直接写 `Geometry.xxx` 即可）。

## 检查项与判据

| # | 检查项 | 判据 | 为什么这样判 |
|---|--------|------|--------------|
| 1 | 图元读取 | 线>500、盘>300 | 通道通了、数据拿得到 |
| 2 | 工作框自适应 | 取 100mil 网格中**最密的一格**，不够就扩大 | 板中心往往是空的，写死"板中心"只采到 12 条线 |
| 3 | 局部预筛 | 框内障碍数 << 全板数 | 全板遍历会锁死 EDA 主线程 |
| 4 | 正向对照 | 框内既有线**中点**，本网必须判"畅通" | 板子已 DRC 0 违规 ⇒ 用更宽松阈值(5mil)时不该有阻挡；有阻挡=几何模型错了 |
| 5 | 反向对照 | 同一批点，换成**不存在的网名**必须判"被挡" | 防止 `free()` 恒真造成假通过 |
| 6 | A* 布线 | 挑一条既有线（40~300mil），以它为中心单独开框，必须算出折线且首尾贴合 | ★框必须装下两个端点，否则 `routeASTAR` 合理地返回 null |
| 7 | 过孔择位（dry-run） | 必须返回候选且 `slack>=0`、且压住本网铜 | 验证"择孔"逻辑；dry-run 不创建图元 |
| 8 | 既有过孔合法性 | 框内既有过孔在 5mil/6mil 判据下全部合法 | 引擎模型与 DRC 互证 |
| 9 | 全板孔到孔 | 暴力扫全部过孔+通孔焊盘，最小孔壁间距 ≥ 0.25mm | 厂规；不靠"最紧约束是谁"反推 |

## 本轮结果（PASS，10/10）

```
图元读取: 线1920 焊盘650 过孔268
几何构建: 框内 线174 盘33 孔21        局部预筛生效: 174 / 全板 1920
正向对照: 101/101 条既有线中点判畅通, 同层误判 0
反向对照: 101/101 点判"被挡"
A* 单层布线可算: 折线 2 点, net UART0_RX, 296.3mil
过孔择位(dry-run): 候选 1618.2,1962.6 余量 0.07mil
既有过孔: 框内 21 个全部合法 (最小余量 1mil)
全板孔到孔: 331 孔, 最小 0.312mm (12.3mil) vs 0.25mm 厂规 → 达标
```

## 自检抓出的三个真 bug（已修）

### 1. 焊盘 `rotation` 是弧度，不是度 ★★
`getAll()` 返回 `rotation: -1.5707963267948966`（精确 −π/2 = 90°）。引擎按"度"处理（`×π/180`），
于是 90° 旋转的 `["OVAL",24.8,88.6]` 长条焊盘**阻挡区方向整个转反**。

- 症状：密集区"明明没挡却判挡住"、A* 找不到路 —— 和"真的布不下"极难区分
- 证据：正向对照 修前 **85/101**，修后 **101/101**
- 修法：`rot = p.rotation` 直接用，不要 ×π/180

### 2. `viaSlack` 把自己算成障碍
`upd(d - v.diameter/2 - r - CL, ...)` 在 `d=0`（就是被测的那个孔）时给出 `-21mil`（正好 = 8+8+5）。
另外**同网过孔的铜**也不该算间距（同网本来就该连在一起），但**同网孔到孔必须算**。

- 症状：修复前"框内 2 个既有过孔全部违规"✗ → 全是假报警
- 修法：`if (d<1e-6) continue;` + 同网时跳过铜间距项、保留孔到孔项

### 3. A* 测试样本的终点在框外 + 哨兵值自欺
`routeASTAR` 要求起终点都在框内，否则返回 `null`（这是**正确**行为）。
但自检只检查了线段的*起点*是否在框内，挑到一条终点在框外的线 ⇒ 误报"布线器坏了"。
真正原因由取证脚本 `tools/_diag.js` 定位：沿线每 2% 采样，**0 处阻挡** —— 障碍物根本没问题。

另外 `minHH===1e9 ? -1 : minHH` 这种魔法哨兵值，把"没有一条过孔的最紧约束是孔到孔"
显示成了 `-1`，于是一块**合格**的板（真实最小孔间距 12.3mil = 0.312mm）被误报成"差 1mil 不达标"✗

- 修法：哨兵值用 `null`/独立字段；关键指标直接**暴力全对全**算真值

## 相关文档

- 原理图改动后怎么办 → `docs/ECO_CHANGE.md`（变更与换料流程，含 X2 晶振 12.5pF→7pF 实战案例）
- 工具包怎么用 → `README.md`；踩过的坑 → `docs/PITFALLS.md`

## 覆盖不到的地方（别把自检当全部）

- 自检**不跑** EasyEDA 官方 DRC（那要几十秒且会改写状态），投板前仍需 `python pcbai.py drc`
- 自检**不验**自由布线器（Freerouting）输出、不验 SES 导入回写
- 自检只确认"几何模型与 DRC 互证"，**不评判设计余量**是否够好（例如某处只剩 1mil 余量是合法的，但不宽裕）
- `tools/_diag*.js` 是取证脚本，遇到可疑结论时拿它把真实数值打出来，别猜
